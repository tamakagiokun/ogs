レスポンスのフォント指定するやつ

.response {
            white-space: pre-wrap;
            margin-top: 10px;
            font-family: 'Arial', sans-serif; /* フォントを指定 */
            font-size: 14px; /* フォントサイズを指定 */
            color: #333; /* 文字色を指定 */
        }

        